services:
  - type: web
    name: digit-classifier
    env: python
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.2
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
